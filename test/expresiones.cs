coso a : entero = 10;
coso b : entero = 3;
coso resultado : entero = a + b * 2;
mostrar(resultado);

coso division : real = 10.0 / 3.0;
mostrar(division);

coso verdad : logico = verdadero;
coso mentira : logico = falso;
mostrar(verdad && mentira);
mostrar(verdad || mentira);

coso mayor : logico = a > b;
mostrar(mayor);
