coso contador : entero = 0;

repetir {
    mostrar(contador);
    contador = contador + 1;
} hasta (contador == 5);

mostrar("---");

si (contador > 3) {
    mostrar("re copado");
} sino {
    mostrar("dale de nuevo");
}
